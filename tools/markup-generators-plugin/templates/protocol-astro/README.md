# Minecraft Protocol Docs

This Astro project was generated from Minecraft protocol schema metadata.

```sh
npm install
npm run dev
```

Run `npm run build` to create the static site in `dist/`. The normalized metadata consumed by the site is in `src/data/protocol.json`.