name: Release Changes

on: 
  push:
    branches:
      - main
      - notifyOnFail

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Throw a failure
      - name: Create failure
        run: exit 1

      # Handle failure and notify
      - name: Handle failure
        if: failure()
        run: |
          echo "Release failed! Issues encountered"
          if ([string]::IsNullOrEmpty(${{secrets.TEST_SECRET}})) {
            echo "Could not find secret"
          }
          else {
            echo "Secret Found"
          }